# Local preview

If the repository preview server is running, open http://127.0.0.1:4180/c/.

To serve the extracted package instead, run:

    python3 -m http.server 8080 --directory course

Then open http://127.0.0.1:8080/. This is a local review snapshot; there is no public deployment URL for these blocked candidates.
