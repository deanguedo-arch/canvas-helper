import {chromium,expect} from '/Users/deanguedo/Documents/GitHub/canvas-helper/node_modules/@playwright/test/index.mjs';
import {mkdir} from 'node:fs/promises';
const evidence='projects/resources/biology30-production/v1/pilot2/verification/2026-09-07-inline-powerpoint-videos';await mkdir(evidence,{recursive:true});
const browser=await chromium.launch();try{for(const [u,topic] of [['b','male-reproductive-structures'],['c','chromosomes-and-dna'],['d','hardy-weinberg']]){const page=await browser.newPage({viewport:{width:1440,height:1000}});await page.goto(`http://127.0.0.1:4180/${u}/#${u}-topic-${topic}`);const lesson=page.locator(`#${u}-topic-${topic}`),jump=lesson.locator('.lesson-model-link a[data-pilot2-return-focus*="-source-video-"]').first();await jump.click();const inline=lesson.locator('.lesson-source-videos .source-video').first();await expect(inline.locator('iframe')).toBeVisible();await expect(page.locator('.video-library-layout iframe')).toHaveCount(0);await page.screenshot({path:`${evidence}/${u}-inline.png`});await inline.locator('..').getByRole('link',{name:'Open this video in the Video Library',exact:true}).click();await expect(page).toHaveURL(new RegExp(`${u}-video-library$`));await expect(lesson.locator('iframe')).toHaveCount(0);await expect(page.locator('.video-library-layout iframe')).toHaveCount(1);console.log(u,'inline player, header jump, library handoff and hidden-player removal passed');await page.close();}}finally{await browser.close();}
