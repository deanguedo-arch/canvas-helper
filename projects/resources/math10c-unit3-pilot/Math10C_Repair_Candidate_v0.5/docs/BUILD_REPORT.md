# Repair build report — v0.5

## Status

Actual edited source and executable regressions are delivered. This is a **source repair candidate for independent re-audit**, not a completed production implementation. The full MathLive experiment, native-origin concurrency, repository/Studio/CI, actual tenant and human acceptance remain unverified. Capacity-fit acceptance fails for some current-policy witnesses; source guards retain work rather than truncating it.

Source fingerprint: `b9d793e2a352cdcd00a0d4dd0fa1cc17cfb35d0bf96492daeba9fe2b500f1275`. Regenerated standalone SHA-256: `39a94356c549a62742a8aeb2c728cff828a4b15d0119f1b251c1e56fc813d585`.

## Inputs and ownership

The repair bundle's 19 manifest members matched; the enclosed v0.4 candidate's 41 listed members matched. Baseline ZIP: `d5623a216332fce329c9cedc801b04688898df76616320d198b1bdb339fadd08`. Immutable M01–M30: `b59def072b94e8a208cb2fef5418b0898620d96a1966fba3c89fbbfcf4431cb5`, unchanged with historical not_run metadata.

No writable Canvas Helper checkout existed here. Canonical shared files are based on the supplied `d0b4cf731dd180cebee908915772e90687d40215` references; a read-only GitHub codec fetch confirmed the recorded source. There was no installation, commit, push, live repository patch, SCORM package, deployment or assignment submission.

## Implemented repairs

**Migration first.** Frozen v0.4 catalog (1,706 IDs), current immutable catalog (1,716 IDs), explicit schema-2 decoder and schema-3 migration. The generated sentinel at old index 122 remains `gen-positive-1_1_1`. Preserve every raw response, historical verdict, original provenance, draft, first response, exposure bit, selected record and completion. Legacy trig support chronology is marked uncertain rather than invented.

**Saving.** Stable status text node with one native retry; exact publication receipts in the proposed shared bridge; default status rendered before event listeners; current snapshot/completion/revision match required for a current-save claim. Quota, validation, conflicts, oversize, failed writes and failed commits stay latched. The course uses published bridge hooks and has no independent LMS session owner. An exclusive Web Lock precedes canonical edits/publication; no lock means conservative read-only recovery. Null is an initialized baseline. Reconciliation preserves conflicting bytes and last-confirmed work.

**Codec.** Compare the JSON-string contribution of raw and packed candidates, with deterministic raw ties. Keep historical raw/CH10LZ1 decoding and full-envelope guard.

**Mathematics.** Literal-zero/unit/sign/order form normalization, true subtree bounds before cancellation, literal M09 consumption, explicit generator validation and exhaustion. Original broader Unit 3 profiles remain separate from the bounded quadratic contract.

**Teaching and feedback.** Staged fading in 3.5, two non-perfect-root bracket tasks, four further reviewed error-interpretation questions, guaranteed mixed strata, unchecked-draft feedback distinct from historical verdicts, pre-feedback trig support, separately retained ratio/setup/value/units outcomes. Optional paper reasoning does not force the preferred algebra method.

**Access/reporting.** Closed sidebar is inert, wrap excludes links hidden inside closed disclosure groups, focus returns through route/scrim/Escape/resize, native inputs preserve text. Reports distinguish checked history/current draft/version/support and show per-skill summaries. Actual 8-page teaching and 13-page stress-report PDFs were rendered and visually inspected; no out-of-media-box text was detected. The report uses synthetic test data only.

## Preservation

All original **122 authored question objects are field-for-field unchanged**. The eight lesson metadata entries, all 22 page routes, all semantic edit keys and all eight completion IDs remain. Ten new authored IDs were added after migration was implemented. Every original existing field maximum is unchanged. Removed DOM IDs are limited to the specified unsafe save wrapper (`save-status`) and obsolete recovery controls (`recover-lms`, `recover-local`, `recover-local-download`), replaced by preserved-version recovery UI. These were controls, not question/completion IDs.

## Executed evidence

| Result file / group | Passed | Failed |
|---|---:|---:|
| `repair-core-results.json` | 17 | 0 |
| `repair-codec-results.json` | 8 | 0 |
| `repair-controller-results.json` | 4 | 0 |
| `repair-browser-results.json` | 17 | 0 |
| `repair-additional-browser-results.json` | 8 | 0 |
| `repair-edge-browser-results.json` | 5 | 0 |
| `independent-authored-oracles.json` | 132 | 0 |
| `worked-identity-oracles.json` | 54 | 0 |

The original fixture binding script passed 11 literal mathematical cases and 14 **separate** future-family refusal guards. Behavioral fixtures point to named browser/state procedures; they are not counted as prose passes. Five actual added/shared test bodies also passed under isolated TypeScript erasure. Neither those five bodies nor emitted-bridge checks are the full repository command. Large generated-case counts are not independent audits of pedagogy.

The focused runner's first invocation hit the outer execution-time limit after command 9. Completed logs were retained; commands 10–14 were explicitly resumed and passed. Later targeted selected-evidence/reveal bindings were run separately. Every aggregate above is derived from actual result files; no timed-out segment is claimed passed by inference.

**Environment:** v22.16.0, Python 3.13.5, TypeScript 5.8.3, Chromium 144.0.7559.96 built on Debian GNU/Linux 13 (trixie). Browser source and DOM are real; localStorage, LockManager and SCORM API are explicit doubles for controlled cases. SetValue stages data and only Commit confirms in the double. Native localhost navigation was actually attempted and returned `ERR_BLOCKED_BY_ADMINISTRATOR`. No browser policy was bypassed.

## Capacity and unresolved items

See `CAPACITY_DECISION.md`. The UI-generated witness fits, but schema-admitted full-state witnesses exceed the unchanged application budget; adversarial provenance can exceed the actual envelope too. Guards/recovery passing does not mean the accepted-state capacity contract fits. No shorter explanation or history policy was selected.

Real MathLive 0.110.0 and fonts could not be obtained in this execution environment. An executable dependency-gated spike is supplied; it recorded BLOCKED rather than substituting a fake component. The optional adapter remains disabled. Full accessibility and actual teacher routes are unaccepted.

## Shared patch

Five production paths: bridge, codec, two repository test files, additive API documentation. Patch SHA-256 `dca943019a8b33f83da168be4d2eee4862a2a12ff5b795f83c824681390eb966`. `git apply --check` and exact reconstruction passed **only on a disposable mirror of the included references**. The live repository patch remains unapplied. Do not claim a tests/reference edit repaired production; that file is unchanged.

## Source of truth / fragile boundaries

Canonical instruction/hints/labels remain `workspace/index.html`; CSS remains `workspace/styles.css`. Current candidate behavior modules are under `workspace/assets/`; `catalog-v04.json`, registry and legacy decoder must not be reordered or regenerated from current display order. On actual integration declare one Math module owner and generated workspace copies. Do not regrade old evidence, regenerate routine HTML or silently enable MathLive. Use the existing bridge's one session owner.

## Deferred checks

Actual `course:create`, current checkout status/typecheck/full shared test command, Studio Edit/apply/reload/Undo, separate rebuild-survival proof, exact-head CI, production exporter, tenant resume/attempt/device/failure/reporting, physical mobile keyboards, VoiceOver/NVDA and learner observation all remain unrun.

**Next gate:** independent source re-audit and separately authorized repository integration by Codex/Dean's operator, using this v0.5 bundle and the unapplied patch. Exact first file: `docs/CODEX_HANDOFF.md`.

Final targeted rerun after optional input-adapter hardening: unreadable native entry, structured root/fading controls and narrow menu checks passed. The disabled MathLive adapter now skips hidden structured-field serialization inputs; this source guard is not real-library acceptance.
