# Chemistry 30 Quiz Image Export

This workspace preserves the extraction contract for the supplied Brightspace
package. The project-local builder reads the original ZIP, extracts each QTI
quiz, labels image assets by quiz/question/context, and produces a
teacher-readable ZIP package.
