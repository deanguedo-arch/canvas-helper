# English Language Arts 10-2 DOCX Export

This workspace is for converting the supplied Brightspace export into Word
documents grouped by Brightspace module.

The source package is retained by path in project metadata because the original
ZIP is large. The manifest copied into `raw/` is the active structure reference
for the generated DOCX files and export audit.
