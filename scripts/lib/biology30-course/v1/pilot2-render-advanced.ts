import type {Instruction} from './pilot2-instruction-audit.js';
import {topicHtml as h} from './pilot2-render-common.js';
export function renderTopicAdvanced(advanced:Instruction['parts'][number]['advanced'],minutes:number|undefined){
  const e=advanced.evidence,r=advanced.related;
  if(advanced.structureVersion!=='a-style-v1'||advanced.paragraphs.length!==3||!advanced.connection||!e||!r||!minutes)throw Error(`Missing authored Advanced Learning structure/time: ${advanced.id}`);
  return `<details class="p2-advanced advanced-learning advanced-learning-block" id="${h(advanced.id)}" data-advanced-block-id="${h(advanced.id)}"><summary><span class="advanced-label">Advanced Learning</span><span class="advanced-summary-title">${h(advanced.title)}</span><span class="advanced-time">${minutes} minutes</span></summary><div class="advanced-learning-body"><h3>${h(advanced.title)}</h3><p class="advanced-connection"><strong>Build from the core idea:</strong> ${h(advanced.connection)}</p>${advanced.paragraphs.map(p=>`<p>${h(p)}</p>`).join('')}<div class="advanced-evidence"><p>${h(e.description)}</p><div class="p2-table-scroll" role="region" aria-label="${h(e.caption)}" tabindex="0"><table><caption>${h(e.caption)}</caption><thead><tr>${e.columns.map(c=>`<th scope="col">${h(c)}</th>`).join('')}</tr></thead><tbody>${e.rows.map(row=>`<tr>${row.map((cell,i)=>i?`<td>${h(cell)}</td>`:`<th scope="row">${h(cell)}</th>`).join('')}</tr>`).join('')}</tbody></table></div><p><strong>Limitation:</strong> ${h(e.limitation)}</p></div><p><a href="#${h(r.routeId)}" data-pilot2-return-route="${h(r.routeId)}" data-pilot2-return-focus="${h(r.focusId)}">${r.kind==='model'?'Models and Data Lab':'Related worked example'}: ${h(r.label)}</a></p><label><input type="checkbox" data-pilot2-optional-flag="${h(advanced.id)}-complete"> Mark this optional explanation complete</label></div></details>`;
}
