Focus only on Brightspace CSV export.

Rules:
- map only from canonical schema
- validate before export
- fail loudly on unsupported structures
- update exporter tests and expected fixtures with any mapping change
