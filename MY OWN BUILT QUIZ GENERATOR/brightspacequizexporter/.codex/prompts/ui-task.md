Focus only on the React UI under `src/app/`.

Rules:
- do not change schema or export behavior unless explicitly required
- keep UI changes local and understandable
- surface validation and review state clearly
